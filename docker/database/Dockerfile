FROM mysql:latest

ARG DB_DATABASE
ARG DB_USER
ARG DB_PASSWORD
ARG DB_ROOT_PASSWORD

ENV MYSQL_DATABASE=${DB_DATABASE} \
    MYSQL_USER=${DB_USER} \
    MYSQL_PASSWORD=${DB_PASSWORD} \
    MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD}

COPY docker/database/initFiles /docker-entrypoint-initdb.d